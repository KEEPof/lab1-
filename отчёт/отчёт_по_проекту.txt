================================================================================
                    ОТЧЁТ ПО ПРОЕКТУ
                    Godot C++ Template: Реализация рекурсивных алгоритмов
================================================================================

СОДЕРЖАНИЕ

1. Введение
2. Назначение и область применения
3. Цель разработки
4. Список сокращений и обозначений
5. Требования к программе
   5.1. Функциональные требования
   5.2. Требования к интерфейсу
   5.3. Требования к реализации
   5.4. Требования к надёжности
   5.5. Требования к тестированию
   5.6. Требования к установке
   5.7. Требования к сопровождению
   5.8. Требования к безопасности
   5.9. Требования к документации
6. Заключение
7. Приложение А. Блок-схемы алгоритмов
8. Приложение Б. Листинги кода
9. Приложение В. Примеры использования

================================================================================
1. ВВЕДЕНИЕ
================================================================================

В данном отчёте представлена разработка программного модуля на языке C++ для
движка Godot 4 с использованием технологии GDExtension. Модуль реализует
рекурсивные и итеративные алгоритмы вычисления математических функций с
подсчётом метрик производительности.

Разработка выполнена в рамках учебного проекта по дисциплине «Программирование»
в Новгородском государственном университете имени Ярослава Мудрого.

================================================================================
2. НАЗНАЧЕНИЕ И ОБЛАСТЬ ПРИМЕНЕНИЯ
================================================================================

Программный модуль предназначен для:

1. Демонстрации различных подходов к реализации рекурсивных алгоритмов
2. Сравнения производительности рекурсивных и итеративных версий
3. Изучения работы с GDExtension для интеграции C++ кода с Godot
4. Визуализации процесса вычислений через графический интерфейс Godot

Область применения:
- Учебные цели (лабораторные работы по программированию)
- Демонстрация возможностей Godot C++
- Тестирование производительности алгоритмов

================================================================================
3. ЦЕЛЬ РАЗБОТКИ
================================================================================

Целью разработки является создание программного модуля на C++ для Godot 4,
реализующего:

1. Рекурсивные алгоритмы вычисления математических функций
2. Итеративные версии тех же алгоритмов
3. Подсчёт метрик производительности:
   - количество вызовов рекурсии
   - время выполнения
   - использование памяти
4. Интеграцию с Godot через GDExtension
5. Графический интерфейс для взаимодействия с пользователем

================================================================================
4. СПИСОК СОКРАЩЕНИЙ И ОБОЗНАЧЕНИЙ
================================================================================

GDExtension    — технология расширения Godot для подключения внешних библиотек
C++            — язык программирования общего назначения
DLL            — динамически подключаемая библиотека (Dynamic Link Library)
GDScript       — скриптовый язык Godot
RefCounted     — базовый класс Godot с подсчётом ссылок
API            — интерфейс прикладного программирования
n              — входной параметр алгоритма (целое число)
i64, i32       — 64-битные и 32-битные целочисленные типы
us             — микросекунды (единица измерения времени)

================================================================================
5. ТРЕБОВАНИЯ К ПРОГРАММЕ
================================================================================

5.1. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
------------------------------

Программа должна реализовывать следующие функции:

5.1.1. Рекурсия 1 (recursion1)
------------------------------
Вычисление функции по формуле:
- f(1) = 1
- f(n) = n + f(n-1), если n чётное
- f(n) = f(n-1) + 2*f(n-2), если n нечётное

Входные данные: целое число n > 0
Выходные данные: результат вычисления, количество вызовов, время выполнения

5.1.2. Рекурсия 2 (recursion2)
------------------------------
Вычисление функции по формуле:
- f(n) = 1, если n < 3
- f(n) = f(n-1) + f(n-2), если n нечётное
- f(n) = Σf(i) для i=1..n-1, если n чётное

Входные данные: целое число n > 0
Выходные данные: результат вычисления, количество вызовов, время выполнения

5.1.3. Рекурсия 3 (recursion3) - дополнительное задание
-------------------------------------------------------
Вывод цифр числа по одной в прямом порядке.

Входные данные: целое число n > 0
Выходные данные: строка с цифрами числа

5.1.4. Итерация 1 (iteration1)
------------------------------
Итеративная версия recursion1.

5.1.5. Итерация 2 (iteration2)
------------------------------
Итеративная версия recursion2.

5.2. ТРЕБОВАНИЯ К ИНТЕРФЕЙСУ
----------------------------

5.2.1. Входной интерфейс
------------------------
Пользователь вводит целое число n через поле ввода в графическом интерфейсе
Godot.

5.2.2. Выходной интерфейс
-------------------------
Результаты отображаются в виде:
- вычисленное значение
- количество вызовов рекурсии
- время выполнения
- использование памяти

5.2.3. Обработка ошибок
-----------------------
При вводе n <= 0 выводится сообщение об ошибке:
"n must be > 0"

5.3. ТРЕБОВАНИЯ К РЕАЛИЗАЦИИ
----------------------------

1. Язык реализации: C++17
2. Движок: Godot 4.x
3. Технология интеграции: GDExtension
4. Система сборки: SCons
5. Платформа: Windows (MSVC)

Структура исходного кода:
```
src/
├── Lab1.h/cpp              — основной класс с алгоритмами
├── RecursionResult.h/cpp   — структура результата
├── example_class.h/cpp     — пример класса
├── register_types.h/cpp    — регистрация типов
└── types.h                 — определения типов
```

5.4. ТРЕБОВАНИЯ К НАДЁЖНОСТИ
----------------------------

1. Корректная обработка некорректных входных данных
2. Отсутствие утечек памяти (использование RefCounted)
3. Стабильная работа при больших значениях n
4. Возврат понятных сообщений об ошибках

5.5. ТРЕБОВАНИЯ К ТЕСТИРОВАНИЮ
------------------------------

1. Покрытие unit-тестами всех основных функций
2. Тестирование граничных значений (n=1, n=0, n<0)
3. Сравнение результатов рекурсивных и итеративных версий
4. Использование фреймворка doctest

5.6. ТРЕБОВАНИЯ К УСТАНОВКЕ
---------------------------

1. Наличие Godot 4.x
2. Установка SCons: pip install scons
3. Наличие компилятора MSVC (Visual Studio Build Tools)
4. Клонирование репозитория
5. Сборка командой: scons platform=windows target=template_debug

5.7. ТРЕБОВАНИЯ К СОПРОВОЖДЕНИЮ
-------------------------------

1. Документирование всех публичных методов
2. Наличие README с инструкциями
3. Модульная архитектура для лёгкого расширения

5.8. ТРЕБОВАНИЯ К БЕЗОПАСНОСТИ
------------------------------

1. Проверка входных данных на корректность
2. Отсутствие работы с памятью вне RefCounted
3. Нет доступа к файловой системе

5.9. ТРЕБОВАНИЯ К ДОКУМЕНТАЦИИ
------------------------------

1. README.md с быстрым стартом
2. IMPLEMENTATION.md с описанием классов
3. BUILD.md с инструкциями по сборке
4. Данный отчёт

================================================================================
6. ЗАКЛЮЧЕНИЕ
================================================================================

В ходе разработки был создан программный модуль на C++ для Godot 4, который:

1. Реализует 3 рекурсивных алгоритма различной сложности
2. Предоставляет итеративные версии для сравнения производительности
3. Автоматически подсчитывает метрики (время, вызовы, память)
4. Интегрирован с Godot через GDExtension
5. Имеет графический интерфейс для удобного тестирования
6. Покрыт unit-тестами с использованием doctest

Проект демонстрирует возможности современной разработки под Godot с
использованием C++ и может служить шаблоном для создания собственных
GDExtension модулей.

================================================================================
7. ПРИЛОЖЕНИЕ А. БЛОК-СХЕМЫ АЛГОРИТМОВ
================================================================================

А.1. Блок-схема алгоритма recursion1 (итеративная версия)
----------------------------------------------------------
[См. файл: блоксхемы/Приложение А - задание 12 итерация.drawio.png]

А.2. Блок-схема алгоритма recursion2 (итеративная версия)
----------------------------------------------------------
[См. файл: блоксхемы/Приложение А - задание 4 итерация.drawio.png]

А.3. Блок-схема алгоритма recursion1 (рекурсивная версия)
----------------------------------------------------------
[См. файл: блоксхемы/Приложение А - задание 12.drawio.png]

А.4. Блок-схема алгоритма recursion2 (рекурсивная версия)
----------------------------------------------------------
[См. файл: блоксхемы/Приложение А - задание 4.drawio.png]

А.5. Блок-схема алгоритма recursion3 (дополнительное задание)
-------------------------------------------------------------
[См. файл: блоксхемы/Приложение А - рекурсия 3(доп задание).drawio.png]

================================================================================
8. ПРИЛОЖЕНИЕ Б. ЛИСТИНГИ КОДА
================================================================================

Б.1. Заголовочный файл Lab1.h (основные методы)
-----------------------------------------------

```cpp
class Lab1 : public RefCounted {
    GDCLASS(Lab1, RefCounted)

private:
    static String format_duration_us(u64 us) {
        if (us < 1000)
            return String::num_uint64(us) + " us";
        if (us < 1'000'000)
            return String::num(us / 1e3, 2) + " ms";
        return String::num(us / 1e6, 3) + " s";
    }

protected:
    static void _bind_methods();

public:
    Lab1() = default;
    ~Lab1() override = default;

    // РЕКУРСИЯ 1
    i64 recursion_internal1(i32 n, i32 &call_count,
                            i32 current_depth, i32 &max_depth) {
        call_count++;
        current_depth++;
        if (current_depth > max_depth)
            max_depth = current_depth;

        if (n == 1)
            return 1;

        if (n % 2 == 0)
            return n + recursion_internal1(n - 1, call_count, current_depth, max_depth);

        return recursion_internal1(n - 1, call_count, current_depth, max_depth)
             + 2 * recursion_internal1(n - 2, call_count, current_depth, max_depth);
    }

    Ref<RecursionResult> recursion1(i32 n) {
        Ref<RecursionResult> result;
        result.instantiate();

        int call_count = 0;
        int max_depth = 0;

        if (n <= 0) {
            result->success = false;
            result->value = 0;
            result->calls = 0;
            result->error = "n must be > 0";
            return result;
        }

        u64 start = Time::get_singleton()->get_ticks_usec();
        i64 value = recursion_internal1(n, call_count, 0, max_depth);
        u64 end = Time::get_singleton()->get_ticks_usec();

        result->success = true;
        result->value = value;
        result->calls = call_count;
        result->error = "";
        result->time = format_duration_us(end - start);

        u64 frame_size = sizeof(i32) + sizeof(i32) + sizeof(i32) + 
                         sizeof(i32) + sizeof(i64);
        result->memory_amount = max_depth * frame_size;

        return result;
    }

    // РЕКУРСИЯ 2
    i64 recursion_internal2(i32 n, i32& call_count,
                             i32 current_depth, i32& max_depth) {
        call_count++;
        current_depth++;
        if (current_depth > max_depth)
            max_depth = current_depth;

        if (n < 3)
            return 1;

        if (n % 2 != 0)
            return recursion_internal2(n - 1, call_count, current_depth, max_depth)
                 + recursion_internal2(n - 2, call_count, current_depth, max_depth);

        i64 sum = 0;
        for (int i = 1; i <= n - 1; i++) {
            sum += recursion_internal2(i, call_count, current_depth, max_depth);
        }
        return sum;
    }

    Ref<RecursionResult> recursion2(i32 n) {
        // ... аналогично recursion1
    }

    // ИТЕРАЦИЯ 1
    i64 iteration_internal1(i32 n) {
        if (n == 1)
            return 1;
        if (n == 2)
            return 3;

        i64 prev2 = 1;
        i64 prev1 = 3;

        for (int i = 3; i <= n; i++) {
            i64 current;
            if (i % 2 == 0)
                current = i + prev1;
            else
                current = prev1 + 2 * prev2;
            prev2 = prev1;
            prev1 = current;
        }

        return prev1;
    }

    // ИТЕРАЦИЯ 2
    i64 iteration_internal2(i32 n) {
        if (n < 3)
            return 1;

        i64 f1 = 1;
        i64 f2 = 1;
        i64 total = 2;

        for (int i = 3; i <= n; i++) {
            i64 f_i;
            if (i % 2 != 0) {
                f_i = f2 + f1;
            } else {
                f_i = total;
            }
            total += f_i;
            f1 = f2;
            f2 = f_i;
        }

        return f2;
    }
};
```

Б.2. Файл регистрации методов Lab1.cpp
--------------------------------------

```cpp
#include "Lab1.h"

void Lab1::_bind_methods() {
    UtilityFunctions::print("!!! Lab1::_bind_methods CALLED !!!");
    ClassDB::bind_method(D_METHOD("recursion1", "n"), &Lab1::recursion1);
    ClassDB::bind_method(D_METHOD("recursion2", "n"), &Lab1::recursion2);
    ClassDB::bind_method(D_METHOD("recursion3", "n"), &Lab1::recursion3);
    ClassDB::bind_method(D_METHOD("iteration1", "n"), &Lab1::iteration1);
    ClassDB::bind_method(D_METHOD("iteration2", "n"), &Lab1::iteration2);
}
```

Б.3. Заголовочный файл RecursionResult.h
----------------------------------------

```cpp
class RecursionResult : public godot::RefCounted {
    GDCLASS(RecursionResult, godot::RefCounted)

protected:
    static void _bind_methods();

public:
    bool success = false;
    i64 value = 0;
    int calls = 0;
    godot::String error = godot::String();
    godot::String time = godot::String();
    godot::String digits_output = godot::String();
    u64 memory_amount = 0;

    RecursionResult() = default;
    ~RecursionResult() override = default;

    bool get_success() const { return success; }
    i64 get_value() const { return value; }
    i32 get_calls() const { return calls; }
    godot::String get_error() const { return error; }
    godot::String get_time() const { return time; }
    godot::String get_digits_output() const { return digits_output; }
    u64 get_memory_amount() const { return memory_amount; }
};
```

Б.4. Регистрация типов register_types.cpp
-----------------------------------------

```cpp
#include "register_types.h"
#include "Lab1.h"
#include "RecursionResult.h"
#include <gdextension_interface.h>

void register_lab1_types() {
    ClassDB::register_class<Lab1>();
    ClassDB::register_class<RecursionResult>();
}

void unregister_lab1_types() {
    // Очистка при выгрузке
}

extern "C" {
    GDExtensionBool GDE_EXPORT lab1_library_init(
            GDExtensionInterfaceGetProcAddress p_get_proc_address,
            GDExtensionClassLibraryPtr p_library,
            GDExtensionInitialization *r_initialization) {
        
        GDExtensionBinding::InitObject init_obj(
            p_get_proc_address, p_library, r_initialization);
        
        init_obj.register_initializer(register_lab1_types);
        init_obj.register_uninitializer(unregister_lab1_types);
        init_obj.set_minimum_library_initialization_level(MODULE_INITIALIZATION_LEVEL_SCENE);
        
        return init_obj.init();
    }
}
```

================================================================================
9. ПРИЛОЖЕНИЕ В. ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
================================================================================

В.1. Использование в GDScript
-----------------------------

```gdscript
# Создание экземпляра Lab1
var lab = Lab1.new()

# Вычисление recursion1
var result = lab.recursion1(5)

if result.get_success():
    print("Значение: ", result.get_value())
    print("Вызовов: ", result.get_calls())
    print("Время: ", result.get_time())
    print("Память: ", result.get_memory_amount(), " байт")
else:
    print("Ошибка: ", result.get_error())

# Вычисление recursion2
result = lab.recursion2(5)
print("Recursion2: ", result.get_value())

# Вычисление recursion3 (вывод цифр)
result = lab.recursion3(12345)
print("Цифры: ", result.get_digits_output())

# Итеративные версии
result = lab.iteration1(5)
print("Iteration1: ", result.get_value())

result = lab.iteration2(5)
print("Iteration2: ", result.get_value())
```

В.2. Сценарий Godot (lab1.gd)
-----------------------------

```gdscript
extends Control

@onready var n_input = $VBoxContainer/NInput
@onready var result_label = $VBoxContainer/ResultLabel
@onready var calls_label = $VBoxContainer/CallsLabel
@onready var time_label = $VBoxContainer/TimeLabel

var lab = Lab1.new()

func _on_compute_button_pressed():
    var n = int(n_input.text)
    
    var result = lab.recursion1(n)
    
    if result.success:
        result_label.text = "Значение: " + str(result.value)
        calls_label.text = "Вызовов рекурсии: " + str(result.calls)
        time_label.text = "Время: " + result.time
    else:
        result_label.text = "Ошибка: " + result.error
```

В.3. Unit-тесты (doctest/test_lab1.cpp)
---------------------------------------

```cpp
TEST_CASE("recursion1: базовый случай n=1") {
    Lab1 lab;
    auto result = lab.recursion1(1);
    
    CHECK(result->success == true);
    CHECK(result->value == 1);
    CHECK(result->calls == 1);
    CHECK(result->error == "");
}

TEST_CASE("recursion1: чётное n=2") {
    Lab1 lab;
    auto result = lab.recursion1(2);
    
    // recursion1(2) = 2 + recursion1(1) = 2 + 1 = 3
    CHECK(result->success == true);
    CHECK(result->value == 3);
}

TEST_CASE("recursion1 vs iteration1: одинаковые результаты") {
    Lab1 lab;
    
    for (int n = 1; n <= 10; n++) {
        auto rec_result = lab.recursion1(n);
        auto iter_result = lab.iteration1(n);
        
        CHECK(rec_result->value == iter_result->value);
    }
}
```

В.4. Сборка проекта
-------------------

```bash
# Debug сборка
scons platform=windows target=template_debug

# Release сборка
scons platform=windows target=template_release

# Или использовать готовые скрипты
build_debug.bat
build_release.bat
```

================================================================================
СПИСОК ИСПОЛЬЗОВАННОЙ ЛИТЕРАТУРЫ
================================================================================

1. Godot Engine Documentation. URL: https://docs.godotengine.org/

2. Godot C++ Documentation. URL: https://godot-jolt.github.io/godot-cpp/

3. doctest Testing Framework. URL: https://github.com/doctest/doctest

4. Стивенс У.Р. UNIX: Программирование среды. — М.: Вильямс, 2020.

5. Страуструп Б. Язык программирования C++. — 5-е изд. — М.: Вильямс, 2021.

================================================================================
                    КОНЕЦ ОТЧЁТА
================================================================================
