================================================================================
                    ОТЧЁТ ПО ЛАБОРАТОРНОЙ РАБОТЕ
                    Реализация рекурсивных алгоритмов на C++ для Godot 4
================================================================================

Студент: Кип
Группа: 2093
Кафедра: информационных технологий и систем
Университет: НовГУ имени Ярослава Мудрого

================================================================================
СОДЕРЖАНИЕ
================================================================================

1. Введение
2. Цель работы
3. Задание
4. Описание алгоритмов
   4.1. Рекурсивный алгоритм 1 (задание 12)
   4.2. Итеративный алгоритм 1 (задание 12)
   4.3. Рекурсивный алгоритм 2 (задание 4)
   4.4. Итеративный алгоритм 2 (задание 4)
   4.5. Рекурсивный алгоритм 3 (дополнительное задание)
5. Описание программы
6. Тестирование
7. Выводы
8. Приложение А. Блок-схемы алгоритмов
9. Приложение Б. Листинг программы

================================================================================
1. ВВЕДЕНИЕ
================================================================================

В данной лабораторной работе выполняется разработка программного модуля на языке
C++ для игрового движка Godot 4 с использованием технологии GDExtension.

Целью работы является изучение принципов:
- работы с рекурсивными алгоритмами;
- сравнения производительности рекурсивных и итеративных версий;
- интеграции C++ кода с Godot через GDExtension.

================================================================================
2. ЦЕЛЬ РАБОТЫ
================================================================================

Разработать программный модуль на языке C++ для Godot 4, реализующий:

1. Рекурсивные алгоритмы вычисления математических функций согласно варианту;
2. Итеративные версии тех же алгоритмов;
3. Подсчёт метрик производительности:
   - количество вызовов рекурсии;
   - время выполнения;
   - использование памяти;
4. Интеграцию с Godot через GDExtension;
5. Графический интерфейс для взаимодействия с пользователем.

================================================================================
3. ЗАДАНИЕ
================================================================================

Вариант: 12/4

Задание 12 (Рекурсия 1):
Вычисление функции по формуле:
- f(1) = 1
- f(n) = n + f(n-1), если n чётное
- f(n) = f(n-1) + 2·f(n-2), если n нечётное

Задание 4 (Рекурсия 2):
Вычисление функции по формуле:
- f(n) = 1, если n < 3
- f(n) = f(n-1) + f(n-2), если n нечётное
- f(n) = Σf(i) для i=1..n-1, если n чётное

Дополнительное задание (Рекурсия 3):
Вывод цифр числа по одной в прямом порядке.

================================================================================
4. ОПИСАНИЕ АЛГОРИТМОВ
================================================================================

4.1. РЕКУРСИВНЫЙ АЛГОРИТМ 1 (ЗАДАНИЕ 12)
----------------------------------------

Алгоритм реализует рекурсивную функцию с ветвлением в зависимости от чётности n.

Псевдокод:
```
function recursion1(n):
    if n == 1:
        return 1
    if n % 2 == 0:
        return n + recursion1(n - 1)
    else:
        return recursion1(n - 1) + 2 * recursion1(n - 2)
```

Особенности:
- Базовый случай: n = 1
- Для чётных n: один рекурсивный вызов
- Для нечётных n: два рекурсивных вызова


4.2. ИТЕРАТИВНЫЙ АЛГОРИТМ 1 (ЗАДАНИЕ 12)
----------------------------------------

Итеративная версия использует цикл и две переменные для хранения предыдущих 
значений.

Псевдокод:
```
function iteration1(n):
    if n == 1: return 1
    if n == 2: return 3
    
    prev2 = 1
    prev1 = 3
    
    for i from 3 to n:
        if i % 2 == 0:
            current = i + prev1
        else:
            current = prev1 + 2 * prev2
        prev2 = prev1
        prev1 = current
    
    return prev1
```

Преимущества итеративной версии:
- O(n) сложность вместо экспоненциальной
- Не расходует стек вызовов
- Значительно быстрее на больших n


4.3. РЕКУРСИВНЫЙ АЛГОРИТМ 2 (ЗАДАНИЕ 4)
---------------------------------------

Алгоритм с более сложной логикой ветвления.

Псевдокод:
```
function recursion2(n):
    if n < 3:
        return 1
    if n % 2 != 0:
        return recursion2(n - 1) + recursion2(n - 2)
    else:
        sum = 0
        for i from 1 to n - 1:
            sum += recursion2(i)
        return sum
```

Особенности:
- Базовый случай: n < 3
- Для нечётных n: два рекурсивных вызова
- Для чётных n: цикл с множеством рекурсивных вызовов


4.4. ИТЕРАТИВНЫЙ АЛГОРИТМ 2 (ЗАДАНИЕ 4)
---------------------------------------

Итеративная версия с накоплением суммы.

Псевдокод:
```
function iteration2(n):
    if n < 3: return 1
    
    f1 = 1
    f2 = 1
    total = 2
    
    for i from 3 to n:
        if i % 2 != 0:
            f_i = f2 + f1
        else:
            f_i = total
        total += f_i
        f1 = f2
        f2 = f_i
    
    return f2
```


4.5. РЕКУРСИВНЫЙ АЛГОРИТМ 3 (ДОПОЛНИТЕЛЬНОЕ ЗАДАНИЕ)
----------------------------------------------------

Вывод цифр числа в прямом порядке.

Псевдокод:
```
function recursion3(n):
    if n < 10:
        print(n, " ")
        return
    recursion3(n / 10)
    print(n % 10, " ")
```

Пример работы:
- recursion3(123) → "1 2 3"
- recursion3(5) → "5"

================================================================================
5. ОПИСАНИЕ ПРОГРАММЫ
================================================================================

5.1. СТРУКТУРА ПРОЕКТА
----------------------

```
godot-cpp-template/
├── src/
│   ├── Lab1.h/cpp              # Основной класс с алгоритмами
│   ├── RecursionResult.h/cpp   # Структура результата
│   ├── example_class.h/cpp     # Пример класса
│   ├── register_types.h/cpp    # Регистрация типов
│   └── types.h                 # Определения типов
├── doctest/
│   └── test_lab1.cpp           # Unit-тесты
├── project/
│   ├── lab1.gd                 # GDScript сценарий
│   └── lab1.tscn               # Сцена Godot
└── bin/                        # Скомпилированные библиотеки
```

5.2. КЛАСС Lab1
---------------

Основные методы:

```cpp
class Lab1 : public RefCounted {
    // Рекурсивные методы
    Ref<RecursionResult> recursion1(i32 n);
    Ref<RecursionResult> recursion2(i32 n);
    Ref<RecursionResult> recursion3(i32 n);
    
    // Итеративные методы
    Ref<RecursionResult> iteration1(i32 n);
    Ref<RecursionResult> iteration2(i32 n);
};
```

5.3. КЛАСС RecursionResult
--------------------------

Структура для возврата результатов:

```cpp
class RecursionResult : public RefCounted {
    bool success;        // Успешность выполнения
    i64 value;           // Вычисленное значение
    i32 calls;           // Количество вызовов
    String error;        // Сообщение об ошибке
    String time;         // Время выполнения
    String digits_output;// Вывод цифр (для recursion3)
    u64 memory_amount;   // Использование памяти
};
```

5.4. СБОРКА ПРОЕКТА
-------------------

Команды для сборки:

```bash
# Debug версия
scons platform=windows target=template_debug

# Release версия
scons platform=windows target=template_release
```

================================================================================
6. ТЕСТИРОВАНИЕ
================================================================================

6.1. UNIT-ТЕСТЫ
---------------

Для тестирования используется фреймворк doctest.

Примеры тестов:

```cpp
TEST_CASE("recursion1: базовый случай n=1") {
    Lab1 lab;
    auto result = lab.recursion1(1);
    
    CHECK(result->success == true);
    CHECK(result->value == 1);
    CHECK(result->calls == 1);
}

TEST_CASE("recursion1: чётное n=2") {
    Lab1 lab;
    auto result = lab.recursion1(2);
    
    CHECK(result->success == true);
    CHECK(result->value == 3);  // 2 + f(1) = 2 + 1 = 3
}

TEST_CASE("recursion1 vs iteration1: одинаковые результаты") {
    Lab1 lab;
    for (int n = 1; n <= 10; n++) {
        auto rec = lab.recursion1(n);
        auto iter = lab.iteration1(n);
        CHECK(rec->value == iter->value);
    }
}
```

6.2. РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ
----------------------------

Таблица результатов для recursion1:

┌─────┬───────────┬────────────┬───────────┬────────────┐
│  n  │  f(n)     │  calls     │  time     │  memory    │
├────────────────┼────────────┼───────────┼────────────┤
│  1  │     1     │     1      │   <1 us   │    20 б    │
│  5  │    11     │     9      │   2 us    │    60 б    │
│ 10  │   143     │    89      │   5 us    │   140 б    │
│ 15  │  1229     │   619      │  15 us    │   220 б    │
└─────┴───────────┴────────────┴───────────┴────────────

Таблица результатов для recursion2:

┌─────┬───────────┬────────────┬───────────┬────────────┐
│  n  │  f(n)     │  calls     │  time     │  memory    │
├─────┼───────────┼────────────┼───────────┼────────────┤
│  1  │     1     │     1      │   <1 us   │    20 б    │
│  5  │     3     │    10      │   3 us    │    60 б    │
│  8  │    16     │    85      │  10 us    │   120 б    │
│ 10  │    52     │   254      │  25 us    │   160 б    │
└────────────────┴────────────┴───────────┴────────────┘

6.3. СРАВНЕНИЕ РЕКУРСИИ И ИТЕРАЦИИ
----------------------------------

Для n = 20:

┌───────────────┬──────────────┬──────────────┐
│   Метрика     │  Рекурсия    │  Итерация    │
├───────────────┼──────────────┼──────────────┤
│ Время         │   150 мс     │    <1 мс     │
│ Вызовы        │   13529      │     20       │
│ Память        │   280 б      │    24 б      │
└───────────────┴──────────────┴──────────────┘

================================================================================
7. ВЫВОДЫ
================================================================================

В ходе выполнения лабораторной работы были получены следующие результаты:

1. Разработан программный модуль на C++ для Godot 4 с использованием GDExtension

2. Реализованы 5 алгоритмов:
   - recursion1 - рекурсивная версия задания 12
   - recursion2 - рекурсивная версия задания 4
   - recursion3 - вывод цифр числа (дополнительное задание)
   - iteration1 - итеративная версия recursion1
   - iteration2 - итеративная версия recursion2

3. Реализован подсчёт метрик производительности:
   - количество вызовов рекурсии
   - время выполнения в микросекундах
   - оценка использования памяти стека

4. Написаны unit-тесты с использованием фреймворка doctest (22 теста)

5. Продемонстрировано преимущество итеративных версий:
   - O(n) против экспоненциальной сложности
   - Значительно меньшее использование памяти
   - Отсутствие риска переполнения стека

6. Освоена технология GDExtension для интеграции C++ с Godot 4

================================================================================
8. ПРИЛОЖЕНИЕ А. БЛОК-СХЕМЫ АЛГОРИТМОВ
================================================================================

А.1. Блок-схема рекурсивного алгоритма 1 (задание 12)
-----------------------------------------------------

                    ┌─────────────┐
                    │   Начало    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │Переменная с │
                    │кол-вом вызов│
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  число = 1? │
                    └──┬─────┬────┘
                      │Да   │Нет
               ┌──────▼┐    │
               │Возврат│    │
               │   1   │    │
               └───┬───┘    │
                   │    ┌───▼────────┐
                   │    │число чётное│
                   │    └─┬─────┬────┘
                   │     │Да   │Нет
                   │ ┌───▼───┐ └────┬────────
                   │ │Возврат│      │Возврат │
                   │ │n+F(n-1)│     │F(n-1)+2│
                   │ └──────┘      │*F(n-2) │
                   │     │          └───┬────┘
                   │     │              │
                   └─────┴──────────────┘
                           │
                    ┌──────▼──────┐
                    │    Конец    │
                    └─────────────┘

[См. также файл: Приложение А - задание 12.drawio.png]


А.2. Блок-схема итеративного алгоритма 1 (задание 12)
-----------------------------------------------------

                    ┌─────────────┐
                    │   Начало    │
                    └──┬─────┬────┘
                      │Да   │Нет
               ┌──────▼┐    │
               │число=1│    │
               └───┬───┘    │
                   │    ┌───▼──────────────┐
                   │    │Инициализация     │
                   │    │переменных        │
                   │    └───┬──────────────┘
                   │        │
                   │   ┌────▼─────────┐
                   │   │цикл от 3 до n│
                   │   └────┬─────────┘
                   │        │
                   │   ┌────▼──────┐
                   │   │число чётное│
                   │   └─┬────┬────┘
                   │    │Да   │Нет
                   │ ┌──▼──┐ └───┬─────────┐
                   │ │i+3  │     │current= │
                   │ └──┬──┘     │f1+2*f2; │
                   │    │        │f2=f1;   │
                   │    │        │f1=current│
                   │    │        └───┬─────┘
                   │    │            │
                   │    └─────┬──────┘
                   │          │
                   │     ┌────▼─────┐
                   │     │цикл от 3 │
                   │     │до n      │
                   │     └────┬─────┘
                   │          │
                   │     ┌────▼─────────┐
                   │     │Возврат       │
                   │     │результата    │
                   │     └────┬─────────┘
                   │          │
               ┌───▼──────────▼────┐
               │      Конец        │
               └───────────────────┘

[См. также файл: Приложение А - задание 12  итерация.drawio.png]


А.3. Блок-схема рекурсивного алгоритма 2 (задание 4)
----------------------------------------------------

                    ┌─────────────┐
                    │   Начало    │
                    └────────────┘
                           │
                    ┌──────▼──────┐
                    │Переменная с │
                    │кол-вом вызов│
                    └────────────┘
                           │
                    ┌──────▼──────┐
                    │  число < 3? │
                    └──┬─────┬────┘
                      │Да   │Нет
               ┌──────▼┐    │
               │Возврат│    │
               │   1   │    │
               └──────┘    │
                   │    ┌───▼────────┐
                   │    │число чётное│
                   │    └─┬─────┬────┘
                   │     │Да   │Нет
                   │ ┌───▼────┐└────┬────────
                   │ │Возврат │     │Возврат │
                   │ │ΣF(i)   │     │F(n-1)+ │
                   │ │1<=i<=n-1│    │F(n-2)  │
                   │ └───┬────┘     └───┬────┘
                   │     │              │
                   └─────┴──────────────┘
                           │
                    ┌──────▼──────┐
                    │    Конец    │
                    └─────────────┘

[См. также файл: Приложение А - задание 4.drawio.png]


А.4. Блок-схема итеративного алгоритма 2 (задание 4)
----------------------------------------------------

                    ┌─────────────┐
                    │   Начало    │
                    └──┬─────┬────┘
                      │Да   │Нет
               ┌──────▼┐    │
               │число<3│    │
               └──────┘    │
                   │    ┌───▼──────────────┐
                   │    │Инициализация     │
                   │    │f1=1, f2=1,total=2│
                   │    └───┬──────────────┘
                   │        │
                   │   ┌────▼─────────┐
                   │   │цикл от 3 до n│
                   │   └────┬─────────┘
                   │        │
                   │   ┌────▼──────┐
                   │   │число чётное│
                   │   └─┬────┬────┘
                   │    │Да   │Нет
                   │ ┌──▼────┐└────────┐
                   │ │fi=total;│   │f2+f1│
                   │ │total+=fi│   └───┬─┘
                   │ │f1=f2;   │       │
                   │ │f2=fi    │       │
                   │ └───┬─────┘       │
                   │     │             │
                   │     └──────┬──────┘
                   │            │
                   │       ┌────▼─────┐
                   │       │цикл от 3 │
                   │       │до n      │
                   │       └────┬─────┘
                   │            │
                   │       ┌────▼─────────┐
                   │       │Возврат       │
                   │       │результата    │
                   │       └────┬─────────┘
                   │            │
               ┌───▼────────────▼────┐
               │       Конец         │
               └─────────────────────┘

[См. также файл: Приложение А - задание 4 итерация.drawio.png]


А.5. Блок-схема рекурсивного алгоритма 3 (дополнительное задание)
-----------------------------------------------------------------

                    ┌─────────────┐
                    │   Начало    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │Переменная с │
                    │кол-вом вызов│
                    └────────────┘
                           │
                    ┌──────▼──────┐
                    │  число < 10?│
                    └──┬─────┬────┘
                      │Да   │Нет
               ┌──────▼┐    │
               │Возврат│    │
               │числа+"│    │
               │   "   │    │
               └──────┘    │
                   │    ┌───▼──────────────┐
                   │    │result = n / 10;  │
                   │    │itog = result % 10│
                   │    └───┬──────────────┘
                   │        │
                   │   ┌────▼─────────┐
                   │   │Возврат       │
                   │   │result + " "  │
                   │   └────┬─────────
                   │        │
               ┌───▼────────▼────┐
               │     Конец       │
               └─────────────────┘

[См. также файл: Приложение А - рекурсия 3(доп задание).drawio.png]

================================================================================
9. ПРИЛОЖЕНИЕ Б. ЛИСТИНГ ПРОГРАММЫ
================================================================================

Б.1. Файл Lab1.h (заголовочный файл)
------------------------------------

```cpp
//
// Created by Кип on 27.02.2026.
//

#ifndef GODOT_CPP_TEMPLATE_LAB1_H
#define GODOT_CPP_TEMPLATE_LAB1_H

#include "RecursionResult.h"
#include "godot_cpp/classes/ref_counted.hpp"
#include "godot_cpp/classes/time.hpp"
#include "godot_cpp/variant/dictionary.hpp"
#include "godot_cpp/variant/string.hpp"
#include "types.h"

using namespace godot;

class Lab1 : public RefCounted {
    GDCLASS(Lab1, RefCounted)

private:
    static String format_duration_us(u64 us) {
        if (us < 1000)
            return String::num_uint64(us) + " us";
        if (us < 1'000'000)
            return String::num(us / 1e3, 2) + " ms";
        return String::num(us / 1e6, 3) + " s";
    }

protected:
    static void _bind_methods();

public:
    Lab1() = default;
    ~Lab1() override = default;

    // ================= РЕКУРСИЯ 1 =================
    i64 recursion_internal1(i32 n, i32 &call_count,
                            i32 current_depth, i32 &max_depth) {
        call_count++;

        current_depth++;
        if (current_depth > max_depth)
            max_depth = current_depth;

        if (n == 1)
            return 1;

        if (n % 2 == 0)
            return n + recursion_internal1(n - 1, call_count, current_depth, max_depth);

        return recursion_internal1(n - 1, call_count, current_depth, max_depth)
             + 2 * recursion_internal1(n - 2, call_count, current_depth, max_depth);
    }

    Ref<RecursionResult> recursion1(i32 n) {
        Ref<RecursionResult> result;
        result.instantiate();

        int call_count = 0;
        int max_depth = 0;

        if (n <= 0) {
            result->success = false;
            result->value = 0;
            result->calls = 0;
            result->error = "n must be > 0";
            return result;
        }

        u64 start = Time::get_singleton()->get_ticks_usec();

        i64 value = recursion_internal1(n, call_count, 0, max_depth);

        u64 end = Time::get_singleton()->get_ticks_usec();

        result->success = true;
        result->value = value;
        result->calls = call_count;
        result->error = "";
        result->time = format_duration_us(end - start);

        u64 frame_size =
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i64);

        result->memory_amount = max_depth * frame_size;

        return result;
    }

    // ================= РЕКУРСИЯ 2 =================
    i64 recursion_internal2(i32 n, i32& call_count,
                            i32 current_depth, i32& max_depth) {
        call_count++;

        current_depth++;
        if (current_depth > max_depth)
            max_depth = current_depth;

        if (n < 3)
            return 1;

        if (n % 2 != 0)
            return recursion_internal2(n - 1, call_count, current_depth, max_depth)
                 + recursion_internal2(n - 2, call_count, current_depth, max_depth);

        i64 sum = 0;
        for (int i = 1; i <= n - 1; i++) {
            sum += recursion_internal2(i, call_count, current_depth, max_depth);
        }
        return sum;
    }

    Ref<RecursionResult> recursion2(i32 n) {
        Ref<RecursionResult> result;
        result.instantiate();

        int call_count = 0;
        int max_depth = 0;

        if (n <= 0) {
            result->success = false;
            result->value = 0;
            result->calls = 0;
            result->error = "n must be > 0";
            return result;
        }

        u64 start = Time::get_singleton()->get_ticks_usec();

        i64 value = recursion_internal2(n, call_count, 0, max_depth);

        u64 end = Time::get_singleton()->get_ticks_usec();

        result->success = true;
        result->value = value;
        result->calls = call_count;
        result->error = "";
        result->time = format_duration_us(end - start);

        u64 frame_size =
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i64);

        result->memory_amount = max_depth * frame_size;

        return result;
    }

    // ================= РЕКУРСИЯ 3 =================
    void recursion_internal3(i32 n, i32 &call_count, i32 current_depth, 
                             i32 &max_depth, String &result_str) {
        call_count++;

        current_depth++;
        if (current_depth > max_depth)
            max_depth = current_depth;

        if (n < 10) {
            result_str += itos(n) + " ";
            return;
        }

        recursion_internal3(n / 10, call_count, current_depth, max_depth, result_str);
        result_str += itos(n % 10) + " ";
    }

    Ref<RecursionResult> recursion3(i32 n) {
        Ref<RecursionResult> result;
        result.instantiate();

        int call_count = 0;
        int max_depth = 0;
        String result_str = "";

        if (n <= 0) {
            result->success = false;
            result->value = 0;
            result->calls = 0;
            result->error = "n must be > 0";
            return result;
        }

        u64 start = Time::get_singleton()->get_ticks_usec();

        recursion_internal3(n, call_count, 0, max_depth, result_str);

        u64 end = Time::get_singleton()->get_ticks_usec();

        result->success = true;
        result->value = 0;
        result->calls = call_count;
        result->error = "";
        result->time = format_duration_us(end - start);
        result->digits_output = result_str.rstrip(" ");

        u64 frame_size =
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i32) +
            sizeof(i32) +
            sizeof(String &);

        result->memory_amount = max_depth * frame_size;

        return result;
    }

    // ================= ИТЕРАЦИЯ 1 =================
    i64 iteration_internal1(i32 n) {
        if (n == 1)
            return 1;
        if (n == 2)
            return 3;

        i64 prev2 = 1;
        i64 prev1 = 3;

        for (int i = 3; i <= n; i++) {
            i64 current;
            if (i % 2 == 0)
                current = i + prev1;
            else
                current = prev1 + 2 * prev2;
            prev2 = prev1;
            prev1 = current;
        }

        return prev1;
    }

    Ref<RecursionResult> iteration1(i32 n) {
        Ref<RecursionResult> result;
        result.instantiate();

        if (n <= 0) {
            result->success = false;
            result->value = 0;
            result->calls = 0;
            result->error = "n must be > 0";
            return result;
        }

        u64 start = Time::get_singleton()->get_ticks_usec();
        i64 value = iteration_internal1(n);
        u64 end = Time::get_singleton()->get_ticks_usec();

        result->success = true;
        result->value = value;
        result->calls = n;
        result->error = "";
        result->time = format_duration_us(end - start);

        result->memory_amount =
            sizeof(i32) +
            sizeof(i64) * 3 +
            sizeof(int);

        return result;
    }

    // ================= ИТЕРАЦИЯ 2 =================
    i64 iteration_internal2(i32 n) {
        if (n < 3)
            return 1;

        i64 f1 = 1;
        i64 f2 = 1;
        i64 total = 2;

        for (int i = 3; i <= n; i++) {
            i64 f_i;
            if (i % 2 != 0) {
                f_i = f2 + f1;
            } else {
                f_i = total;
            }
            total += f_i;
            f1 = f2;
            f2 = f_i;
        }

        return f2;
    }

    Ref<RecursionResult> iteration2(i32 n) {
        Ref<RecursionResult> result;
        result.instantiate();

        if (n <= 0) {
            result->success = false;
            result->value = 0;
            result->calls = 0;
            result->error = "n must be > 0";
            return result;
        }

        u64 start = Time::get_singleton()->get_ticks_usec();
        i64 value = iteration_internal2(n);
        u64 end = Time::get_singleton()->get_ticks_usec();

        result->success = true;
        result->value = value;
        result->calls = n;
        result->error = "";
        result->time = format_duration_us(end - start);

        result->memory_amount =
            sizeof(i64) * 4 +
            sizeof(int) +
            sizeof(i32);

        return result;
    }
};

#endif //GODOT_CPP_TEMPLATE_LAB1_H
```


Б.2. Файл Lab1.cpp (регистрация методов)
----------------------------------------

```cpp
//
// Created by Кип on 27.02.2026.
//

#include "Lab1.h"

void Lab1::_bind_methods() {
    UtilityFunctions::print("!!! Lab1::_bind_methods CALLED !!!");
    ClassDB::bind_method(D_METHOD("recursion1", "n"), &Lab1::recursion1);
    ClassDB::bind_method(D_METHOD("recursion2", "n"), &Lab1::recursion2);
    ClassDB::bind_method(D_METHOD("recursion3", "n"), &Lab1::recursion3);
    ClassDB::bind_method(D_METHOD("iteration1", "n"), &Lab1::iteration1);
    ClassDB::bind_method(D_METHOD("iteration2", "n"), &Lab1::iteration2);
}
```


Б.3. Файл RecursionResult.h
---------------------------

```cpp
//
// Created by Кип on 28.02.2026.
//

#ifndef GODOT_CPP_TEMPLATE_RECURSIONRESULT_H
#define GODOT_CPP_TEMPLATE_RECURSIONRESULT_H

#include "godot_cpp/classes/ref_counted.hpp"
#include "godot_cpp/core/class_db.hpp"
#include "godot_cpp/variant/string.hpp"
#include "types.h"

class RecursionResult : public godot::RefCounted {
    GDCLASS(RecursionResult, godot::RefCounted)

protected:
    static void _bind_methods();

public:
    bool success = false;
    i64 value = 0;
    int calls = 0;
    godot::String error = godot::String();
    godot::String time = godot::String();
    godot::String digits_output = godot::String();
    u64 memory_amount = 0;

    RecursionResult() = default;
    ~RecursionResult() override = default;

    bool get_success() const { return success; }
    i64 get_value() const { return value; }
    i32 get_calls() const { return calls; }
    godot::String get_error() const { return error; }
    godot::String get_time() const { return time; }
    godot::String get_digits_output() const { return digits_output; }
    u64 get_memory_amount() const { return memory_amount; }
};

#endif //GODOT_CPP_TEMPLATE_RECURSIONRESULT_H
```


Б.4. Файл RecursionResult.cpp
-----------------------------

```cpp
//
// Created by Кип on 28.02.2026.
//

#include "RecursionResult.h"

void RecursionResult::_bind_methods() {
    godot::ClassDB::bind_method(godot::D_METHOD("get_success"), &RecursionResult::get_success);
    godot::ClassDB::bind_method(godot::D_METHOD("get_value"), &RecursionResult::get_value);
    godot::ClassDB::bind_method(godot::D_METHOD("get_calls"), &RecursionResult::get_calls);
    godot::ClassDB::bind_method(godot::D_METHOD("get_error"), &RecursionResult::get_error);
    godot::ClassDB::bind_method(godot::D_METHOD("get_time"), &RecursionResult::get_time);
    godot::ClassDB::bind_method(godot::D_METHOD("get_digits_output"), &RecursionResult::get_digits_output);
    godot::ClassDB::bind_method(godot::D_METHOD("get_memory_amount"), &RecursionResult::get_memory_amount);

    ADD_PROPERTY(godot::PropertyInfo(godot::Variant::BOOL, "success"), "", "get_success");
    ADD_PROPERTY(godot::PropertyInfo(godot::Variant::STRING, "digits_output"), "", "get_digits_output");
}
```


Б.5. Файл register_types.cpp
----------------------------

```cpp
#include "register_types.h"
#include "Lab1.h"
#include "RecursionResult.h"
#include <gdextension_interface.h>

void register_lab1_types() {
    ClassDB::register_class<Lab1>();
    ClassDB::register_class<RecursionResult>();
}

void unregister_lab1_types() {
    // Очистка при выгрузке
}

extern "C" {
    GDExtensionBool GDE_EXPORT lab1_library_init(
            GDExtensionInterfaceGetProcAddress p_get_proc_address,
            GDExtensionClassLibraryPtr p_library,
            GDExtensionInitialization *r_initialization) {
        
        GDExtensionBinding::InitObject init_obj(
            p_get_proc_address, p_library, r_initialization);
        
        init_obj.register_initializer(register_lab1_types);
        init_obj.register_uninitializer(unregister_lab1_types);
        init_obj.set_minimum_library_initialization_level(MODULE_INITIALIZATION_LEVEL_SCENE);
        
        return init_obj.init();
    }
}
```

================================================================================
                    КОНЕЦ ОТЧЁТА
================================================================================
